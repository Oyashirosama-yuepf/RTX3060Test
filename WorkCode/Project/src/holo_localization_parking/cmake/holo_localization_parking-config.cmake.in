find_package(holo_localization_base @HOLO_LOCALIZATION_BASE_VERSION@ REQUIRED)

if(@HOLO_LOOP_WITH_NETVLAD@)
    find_package(holo_localization_deep @HOLO_LOCALIZATION_DEEP_VERSION@ REQUIRED)
endif()

include(${CMAKE_CURRENT_LIST_DIR}/holo_localization_parking-targets.cmake)

